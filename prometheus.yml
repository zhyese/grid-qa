global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: grid-qa-backend
    metrics_path: /metrics
    static_configs:
      # 本地 uvicorn 用 host.docker.internal(容器→宿主机); docker全栈时也可(8001映射)
      - targets: ['host.docker.internal:8001']
