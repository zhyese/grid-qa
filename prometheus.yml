global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: grid-qa-backend
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:8001']   # docker 网络内 backend 服务
