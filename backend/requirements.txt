# ===== S1 地基 + 骨架 =====
fastapi>=0.115,<0.116
uvicorn[standard]>=0.34,<0.35
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
python-dotenv>=1.0,<2

# ===== S2 认证（bcrypt 直连 + PyJWT，规避 passlib/bcrypt4.x 兼容坑）=====
SQLAlchemy[asyncio]>=2.0,<3
aiomysql>=0.2,<0.3
bcrypt>=4.0,<5
PyJWT>=2.8,<3
email-validator>=2.0

# ===== S3 文档上传（form-data 需要 python-multipart）=====
minio>=7.2,<8
python-multipart>=0.0.9

# ===== S4 文档解析与分块 =====
pdfplumber>=0.11
pypdf>=4
python-docx>=1.1
PyMuPDF>=1.24
rapidocr-onnxruntime>=1.2   # PaddleOCR PP-OCR 模型 + onnxruntime 后端（规避 paddle3.3 Win onednn bug）
# 原生 paddleocr/paddlepaddle 在 Linux 生产环境可启用，见 services/parse_service.py 注释

# ===== 后续切片按需追加（暂不安装）=====
# S5 向量化:     pymilvus openai
# S6 检索:       rank-bm25 jieba
