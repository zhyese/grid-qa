# 电网运维 RAG 智能问答系统 · 架构总览（代码级 mermaid）

> **版本**：`980d463` 后重构 mermaid，2026-07-20 ｜ **仓库**：github.com/zhyese/grid-qa
> **技术栈**：Vue3 + FastAPI + MySQL + Redis + Milvus 2.4(双collection) + Neo4j + MinIO + 三家云模型 + Docker Compose(12容器)
> 节点命名约定：`文件.py::函数()` / `表名` / `开关名`

---

## 图 1 · 系统总览（分层 + 主数据流）

```mermaid
flowchart TB
subgraph FE["🖥️ frontend/src/views · 17 视图"]
    Chat["Chat.vue /chat<br/>streamAnswer SSE"]
    Diag["Diagnose.vue<br/>故障诊断"]
    Ops["OperationsCenter.vue<br/>主动运维"]
    Twin["DigitalTwin.vue + three/<br/>3D变电站"]
    DocV["Documents.vue"]
    KG["KgGraph.vue / KgGraph3D.vue"]
    Ticket["TicketLifecycle.vue"]
    KEvo["KnowledgeEvolution.vue<br/>KnowledgeGovernance.vue"]
    Pred["FaultPrediction.vue"]
    Adm["Admin.vue 13Tab"]
end

subgraph GW["🌐 routers/ · 14 · core/ 横切"]
    RQA["routers/qa.py"]
    RSYS["routers/system.py<br/>routers/document.py"]
    RKG["routers/kg.py"]
    REVO["routers/knowledge_evolution.py<br/>routers/knowledge_governance.py"]
    ROPS["routers/twin.py /task_center.py<br/>/realtime_event.py /memory.py<br/>/domain.py /mcp_router"]
    Perm["core/permissions.py::require_perm()<br/>core/security.py::JWT"]
    Safe["core/safety.py::guard_query/safe_answer"]
    Lim["core/limiter.py slowapi"]
    Plug["services/plugin_registry.py 3hook"]
end

subgraph BIZ["⚙️ services/ 编排"]
    QA["qa_service.py::answer/stream_answer<br/>_apply_citation_verification<br/>_schedule_nli_backfill/_nli_backfill"]
    Ret["retrieval_service.py::mixed_search"]
    Crag["rag/crag.py + rag/crag_v2.py<br/>qa_service._crag_correct"]
    Doc["document_service.py"]
    KgS["kg_service.py::graph_context"]
    Agent["agent_personas/persona_store<br/>diagnose_agent_service<br/>debate_agent_service"]
    Evo["knowledge_evolution_service<br/>fault_prediction_service<br/>backup_service/log_archive_service"]
    Dual["rag_router.py 双RAG热备"]
end

subgraph RAGG["🧠 rag/ + routing/ 算法"]
    Cite["rag/citation.py::auto_cite<br/>rag/citation_verifier.py::verify<br/>rag/citation_index.py::build_index"]
    Algo["rag/rrf.py::rrf_fuse<br/>rag/mmr.py::mmr<br/>rag/raptor.py<br/>rag/semantic_cache.py"]
    Strat["hyde.py / multi_query.py<br/>standalone_query.py / self_rag.py<br/>query_rewrite.py::rewrite_query_v2"]
    Rt["routing/routing_service.py::route_query<br/>routing/query_classifier.py"]
end

subgraph DATA["💾 models/ 24表 + clients/"]
    DB[("MySQL · models/*.py")]
    RC[("Redis · clients/redis_client.py<br/>cache_get/set_json")]
    ML[("Milvus · grid_chunks / grid_chunks_bge<br/>/ memory_collection")]
    N4[("Neo4j :Entity/:REL")]
    MN[("MinIO clients/minio_client.py")]
end

subgraph MODEL["☁️ providers/"]
    Fac["providers/factory.py<br/>get_llm/embedding_provider<br/>check_*_health"]
    DS["llm/deepseek<br/>LLM+NLI+Agent"]
    BL["embedding/qwen+bge+doubao<br/>+ rerank + VLM"]
end

subgraph OPS["📊 横切"]
    Prom[("core/metrics.py<br/>Prometheus")]
    Graf["Grafana 18面板"]
    Nac["clients/nacos_client.py"]
    Otel["OTEL→Langfuse"]
end

FE --> GW --> BIZ --> RAGG --> DATA
BIZ -.providers.-> MODEL
BIZ -.metrics.-> OPS
RAGG -.embed/LLM.-> MODEL
```

---

## 图 2 · 问答链路（流式，函数级 + 文件路径）

```mermaid
flowchart TD
    A(["📥 POST /api/qa/answer/stream<br/>routers/qa.py::answer_stream"]):::start
    A --> B["core/safety.py::guard_query()<br/>services/term_service.py::normalize()"]
    B --> Self{"services/self_rag.py::need_retrieve()<br/>SELF_RAG_ENABLE?"}
    Self -->|非运维| Z2(["🚫 refused"]):::refuse
    Self -->|运维/关| C{"qa_service.stream_answer<br/>三级缓存读"}

    C -->|L1 命中| Z1(["🔥 快流 不调LLM"]):::hit
    C -->|miss| D["services/standalone_query.py<br/>多轮指代消解 search_q<br/>STANDALONE_REWRITE_ENABLE"]
    D --> RT["routing/routing_service.py::route_query()<br/>→RoutingDecision(route,features)"]
    RT --> F["retrieval_service.py::mixed_search()"]

    F --> CRAG{"qa_service._crag_correct()<br/>rag/crag_v2.grade_with_llm / rag/crag.grade"}
    CRAG -->|incorrect| RW["query_rewrite.rewrite_query(force=True)<br/>→ mixed_search 重检索"]
    RW --> CRAG2{"仍 incorrect?"}
    CRAG2 -->|是| Z2
    CRAG2 -->|否| F
    CRAG -->|correct/ambiguous| H["kg_service.py::graph_context()<br/>KG_RAG_ENABLE · jieba分词"]

    H --> I["providers/factory.get_llm_provider().stream()<br/>meta→token×N · B4 LLM_USAGE_TRACK_ENABLE"]
    I --> J["rag/citation.py::auto_cite() / STRUCTURED_OUTPUT"]
    J --> K["qa_service._apply_citation_verification()"]

    K --> K1["rag/citation_verifier.py::verify()<br/>校验1 格式 + 校验2 cosine(0.4) 同步<br/>〈A1/A4 chunk_vec 复用〉"]
    K1 --> K2["校验3 NLI 异步后置<br/>_schedule_nli_backfill → _nli_backfill<br/>CITATION_NLI_ASYNC_ENABLE"]
    K2 --> L["drop→警示 · rewrite 联动 CRAG〈C2 合并〉"]
    L --> M["⑧ done 含 citationVerified〈C3〉"]
    M --> N["cache_persist.py 双写 L2+L1<br/>citation_cache_version 隔离"]

    M -.-> BG["_bg_tasks 后台:<br/>_nli_backfill NLI回写 / generate_related<br/>evidence_gap_service.collect / online_eval / cost_tracker"]
    N --> END(["✅ [DONE]"]):::start

classDef start fill:#3b82f6,color:#fff;
classDef hit fill:#10b981,color:#fff;
classDef refuse fill:#ef4444,color:#fff;
```

---

## 图 3 · 检索引擎 mixed_search（8 步代码级 + 路由分支 + 策略矩阵）

```mermaid
flowchart TD
    IN(["mixed_search(db,query,topk,<br/>routing_decision,user_dept/role)"]):::start

    IN --> S0["0️⃣ query_rewrite.rewrite_query_v2()<br/>口语→规范 adaptive+缓存+评估闭环"]
    S0 --> S0b{"0.5 multi_query.decompose()<br/>MULTI_QUERY_ENABLE & route≠sparse<br/>rewrite_cache 缓存"}
    S0b --> S1

    S1{"1️⃣ routing_decision.route<br/>RRF_ROUTE_AWARE_ENABLE?"}
    S1 -->|sparse| P_sparse["仅 bm25_service.search()<br/>SYNONYM_EXPAND_ENABLE 同义词扩展<br/>query_classifier.should_skip_rerank 可跳rerank"]
    S1 -->|dense| P_dense["_hyde_or_cache HyDE<br/>embedding.embed_query 双路(云+bge) asyncio.gather<br/>B6 _ef_for_route(dense+fault→ef×2)<br/>milvus_client.search 双collection"]
    S1 -->|hybrid/sparse_first| P_hybrid["_dense_and_sparse()<br/>dense双路 + BM25<br/>B6 _ef_for_route(sparse→ef//2 floor16)"]

    P_sparse & P_dense & P_hybrid --> S2

    S2{"2️⃣ 融合排序<br/>_rerank_pool_cap A5(topk×1.2)"}
    S2 -->|单路| Sort["按 score 排序取 pool_cap"]
    S2 -->|hybrid| RRF["rag/rrf.py::rrf_fuse()<br/>A2 _rrf_weights_for_route(sparse_first→sparse×1.3)<br/>_aggregate_srcs 归因回填"]
    Sort & RRF --> S3

    S3["3️⃣ rerank_service.get_reranker().rerank()<br/>gte-rerank-v2 · RERANK_ENABLE<br/>高置信sparse可skip"]
    S3 --> S4["4️⃣ 元数据过滤<br/>tenant/docType/equipment<br/>_acl_ok(dept,allowed_roles) RBAC文档级ACL<br/>docType 无条件补全"]
    S4 --> S5["5️⃣ rag/mmr.py::mmr()<br/>A3 _mmr_lambda_for_query_type<br/>(fault.7/mixed.5/natural.4)<br/>MMR_ENABLE"]
    S5 --> S6["6️⃣ _expand_parents small-to-big<br/>小块→同组父块全文<br/>SMALL_TO_BIG_ENABLE"]
    S6 --> S7["7️⃣ rag/raptor.retrieve_with_raptor()<br/>层次摘要 RRF 融合<br/>RAPTOR_ENABLE"]
    S7 --> S75["7.5 知识治理硬门禁<br/>knowledge_governance_service.blocked_document_ids()<br/>撤回/替代/过期 doc 剔除"]
    S75 --> S8["8️⃣ 回填 chunk_id<br/>按 doc_id,chunk_idx 批量查 Chunk<br/>B1 复合索引 ix_chunks_doc_idx"]
    S8 --> OUT(["→ list[dict] contexts"]):::start

classDef start fill:#3b82f6,color:#fff;
```

---

## 图 4 · CRAG 自纠错 + 可核验引用校验（代码级）

```mermaid
flowchart TD
    CTX(["contexts 进入 _crag_correct"]):::start
    CTX --> V2{"CRAG_PERDOC_ENABLE<br/>rag/crag_v2.grade_with_llm()"}
    V2 -->|开+成功| GV2["LLM 逐条评估证据<br/>grade + 明细"]
    V2 -->|关/失败| V1["rag/crag.grade(top1_score)<br/>CRAG_HIGH=0.6 / CRAG_LOW=0.3"]
    GV2 & V1 --> DECIDE{grade}

    DECIDE -->|correct| CONF1["confidence=high action=normal"]
    DECIDE -->|ambiguous| AMB{"CRAG_NEIGHBOR_EXPAND_ENABLE?<br/>_expand_neighbors(±window)"}
    DECIDE -->|incorrect| INC["query_rewrite.rewrite_query(force=True)"]
    INC --> RE["mixed_search 重检索"]
    RE --> REG["crag.grade 复评"]
    REG --> DECIDE2{仍 incorrect?}
    DECIDE2 -->|是| REF(["🚫 refused 保守拒答"]):::refuse
    DECIDE2 -->|否| RW["action=rewritten"]

    AMB --> CONF2["confidence=medium"]
    CONF1 & CONF2 & RW --> MET["metrics.CRAG_GRADE/ACTION/CONFIDENCE"]
    MET --> COUT(["→ contexts,confidence,action,grade"]):::start

    COUT -.送校验.-> CVIN(["_apply_citation_verification<br/>ans + contexts"])

    subgraph CIT ["rag/citation_verifier.py::verify() 三层校验"]
        CVIN --> C1["校验1 格式合法<br/>chunk_id_of(ref_id) ∈ index"]
        C1 --> C2["校验2 cosine 同步<br/>embedding.embed_texts(sent+chunk)<br/>A1/A4 chunk_ids 走缓存<br/>CITATION_VERIFY_SIM_THRESHOLD=0.4"]
        C2 --> C3{"CITATION_NLI_ASYNC_ENABLE?"}
        C3 -->|关 同步| C3S["校验3 NLI 同步<br/>judge._verify_claims<br/>CITATION_NLI_TIMEOUT"]
        C3 -->|开 异步 C1| C3A["同步跳过 NLI<br/>labels=unknown 不阻塞首答"]
        C3A --> BG2["_bg_tasks:<br/>_nli_backfill()<br/>后台 judge._verify_claims<br/>回写缓存 citationVerified.nli"]
        C3S & C3A --> DROP["contradict→drop<br/>替换警示 / rewrite_needed<br/>C2: CRAG rewritten 不二次 rewrite"]
    end
    DROP --> DONE(["→ done 含 citationVerified C3"]):::start

classDef start fill:#3b82f6,color:#fff;
classDef refuse fill:#ef4444,color:#fff;
```

---

## 图 5 · 三级缓存（代码路径 + cv 版本隔离）

```mermaid
flowchart LR
    Q(["query 单轮/standalone"]):::start

    Q --> L1{"L1 Redis<br/>redis_client.cache_get_json<br/>_cache_key(model,nq,tenant)"}
    L1 -->|命中| HIT1["返回 + B2 CACHE_SLIDE_TTL_ENABLE EXPIRE 续期"]
    L1 -->|miss| L2{"L2 MySQL<br/>cache_persist.cache_get_mysql<br/>CACHE_PERSIST_ENABLE<br/>hash 命中"}
    L2 -->|命中| HIT2["返回(被 L1.5 截胡则不到这)"]
    L2 -->|miss| L15{"L1.5 Semantic<br/>semantic_cache.semantic_cache_get<br/>cos≥0.85 SEMANTIC_CACHE_ENABLE"}
    L15 -->|命中| HIT3["返回 semantic_high/medium"]
    L15 -->|miss| LLM["走 LLM 链路"]

    HIT1 & HIT2 & HIT3 --> RET(["✅ 快流返回"]):::hit
    LLM --> WRITE

    subgraph WRITE ["Write-Through 双写（单轮 high 置信）"]
        direction TB
        W1["cache_data 组装<br/>answer/retrievalSource/evidenceTrace/citationVerified"]
        W2["cache_persist.cache_set_mysql L2 先写"]
        W3["redis_client.cache_set_json L1 后写<br/>QA_CACHE_TTL=259200 / 分层TTL"]
        W4["semantic_cache.semantic_cache_set L1.5 索引"]
        W1 --> W2 --> W3 --> W4
    end

    CV["config.citation_cache_version()<br/>= cv{VERIFIER}{STRUCTURED}{NLI}<br/>编进所有缓存键<br/>开关变→key变→旧缓存自动失效"] -.约束.-> L1 & L2 & L15 & W3

classDef start fill:#3b82f6,color:#fff;
classDef hit fill:#10b981,color:#fff;
```

---

## 图 6 · 数据模型 ER（核心 14 表）

```mermaid
erDiagram
  USER ||--o{ DOCUMENT : "owns(admin/editor)"
  USER ||--o{ CONVERSATION : "has"
  USER ||--o{ FEEDBACK : "gives 👍👎"
  USER ||--o{ FAVORITE : "saves"
  USER ||--o{ OPERATION_LOG : "audited"
  USER ||--o{ AGENT_MEMORY : "remembers"
  USER ||--o{ TICKET : "creates/approves"

  DOCUMENT ||--o{ CHUNK : "split into"
  DOCUMENT ||--o{ DOCUMENT_VERSION : "versioned"
  DOCUMENT ||--o{ KG_TRIPLE : "extracted"
  DOCUMENT ||--o{ EVIDENCE_GAP : "missing-info"
  DOCUMENT ||--o{ KNOWLEDGE_GOVERNANCE : "governed"

  CONVERSATION ||--o{ MESSAGE : "contains"

  USER {
    string username PK
    string password_hash
    string role "admin/editor/operator/auditor"
    string dept
    string status "active/disabled"
    string tenant_id
  }
  DOCUMENT {
    int id PK
    string doc_name
    string doc_type "手册/案例/实时/FAQ"
    string dept
    string allowed_roles
    string tenant_id
    string equipment_tags
  }
  CHUNK {
    int id PK
    int doc_id FK
    int chunk_idx
    int parent_idx
    text content
    int page_num
    string section_path
    string bbox
  }
  CONVERSATION {
    int id PK
    string username FK
    string title
    bool deleted
  }
  KG_TRIPLE {
    int id PK
    string subject
    string relation
    string object
    int doc_id FK
  }
  QA_CACHE {
    string cache_key PK
    string query_hash
    json data
    string tenant_id
  }
  FEEDBACK {
    int id PK
    string username FK
    string query
    string fb "like/dislike"
  }
  KNOWLEDGE_GOVERNANCE {
    int id PK
    int doc_id FK
    string status "active/withdrawn/replaced/expired"
  }
  EVIDENCE_GAP {
    int id PK
    string query
    string confidence
  }
  AGENT_MEMORY {
    int id PK
    string username FK
    text content
  }
  TICKET {
    int id PK
    string ticket_type "工作票/操作票"
    string status "draft/审核/签发/执行/归档"
  }
```

> 其余 10 表：rewrite_event / agent_tool_call / persona_config / operation_log / permission(role_permission) / favorite / alert_disposal / domain_event / realtime_event / persistent_task / document_version / knowledge_evolution_drafts。

---

## 图 7 · 高级能力闭环（自进化 / 治理 / Agent / 主动运维 / 孪生 / 双RAG / 备份）

```mermaid
flowchart LR
    subgraph EVO ["🔄 知识自进化 knowledge_evolution_service"]
        D1["用户 dislike<br/>models/feedback"] --> D2["零依赖贪心聚类<br/>→ Milvus 盲区"]
        D2 --> D3["LLM 规程 RAG 草稿<br/>models/knowledge_evolution_drafts"]
        D3 --> D4["KnowledgeEvolution.vue 审核台"]
        D4 -->|通过| D5["回流 source_type=ai_evolution<br/>降权+每周配额+cron+可撤回"]
    end

    subgraph GOV ["🛡️ 知识治理 knowledge_governance_service"]
        G1["时效审查/冲突发现/版本追溯<br/>models/knowledge_governance"] --> G2{"mixed_search 7.5 硬门禁"}
        G2 -->|withdrawn/replaced/expired| G3["剔除出 LLM 上下文"]
        G2 -->|治理存储异常| G4["C5 fail-open 放行+DEGRADED"]
    end

    subgraph AGENT ["🤖 Agent 引擎"]
        A1["agent_personas + persona_store<br/>models/persona_config"] --> A2["diagnose_agent_service 故障诊断<br/>debate_agent_service 辩论"]
        A2 --> A3["Tool/ToolRegistry<br/>agent_tool_call 审计"]
    end

    subgraph OPS2 ["🚨 主动运维"]
        O1["realtime_event webhook<br/>SCADA/Grafana"] --> O2["Agent 建议"]
        O2 --> O3["OperationsCenter 人工确认"]
        O3 --> O4["persistent_task 持久任务"]
        O4 --> O5["fault_prediction 聚合告警→预测"]
    end

    subgraph TWIN ["🌐 数字孪生"]
        T1["twin.py + DigitalTwin.vue<br/>three/deviceFactory+sceneEnvironment"] --> T2["station_layout_110kv.json"]
        T2 --> T3["3D 告警定位 + 故障传播链"]
    end

    subgraph RES ["韧性"]
        R1["rag_router 双RAG热备<br/>DUAL_RAG_ENABLE 主路异常→副路BM25+LLM"]
        R2["backup_service MySQL全表dump→.sql"]
        R3["log_archive_service 90天→jsonl"]
        R4["memory.py + agent_memory<br/>Milvus memory_collection"]
    end

    D5 -.回流.-> GOV
    A3 -.工具.-> OPS2
    O5 -.告警.-> TWIN
```

---

## 附 A · 配置开关速查（config.py 100+，按域）

| 域 | 关键开关（默认） |
|---|---|
| 模型 | LLM_PROVIDER=deepseek · EMB_PROVIDER=qwen · EMBEDDING_DIM=1024 · LLM_USAGE_TRACK_ENABLE(B4) |
| 缓存 | CACHE_PERSIST_ENABLE=T · CACHE_TIERED_TTL_ENABLE=T · CACHE_SLIDE_TTL_ENABLE(B2) · EMBED_CACHE_SLIDE_TTL_ENABLE(B3) · EMBED_CHUNK_CACHE_ENABLE(A1/A4) · SEMANTIC_CACHE_ENABLE |
| GraphRAG | KG_RAG_ENABLE=T · KG_TOKENIZE_CACHE_ENABLE(B5) |
| 检索 | RERANK_ENABLE · MMR_ENABLE/λ=0.5 · RRF_K=60 · RRF_ROUTE_AWARE_ENABLE(A2/A3/A5/B6) |
| 双embed | BGE_DIM=512 · DOC_SIZE_THRESHOLD=5000 · MILVUS_COLLECTION_BGE |
| 分块 | CHUNK_SIZE=500 · SMALL_TO_BIG_ENABLE=T · PARENT_SIZE=2000 |
| CRAG | CRAG_ENABLE=T · CRAG_HIGH=0.6/LOW=0.3 · CRAG_PERDOC_ENABLE(v2) · CRAG_NEIGHBOR_EXPAND_ENABLE |
| 策略 | HYDE/MULTI_QUERY/SELF_RAG/RAPTOR_ENABLE(关) · STANDALONE_REWRITE_ENABLE=T · ROUTING_ENABLE=T |
| 改写 | QUERY_REWRITE_ENABLE · REWRITE_ADAPTIVE_ENABLE=T · REWRITE_EVAL_ENABLE=T |
| 安全 | SAFETY_FILTER_ENABLE=T · PII_MASK_ENABLE · HIGH_RISK_KEYWORDS |
| 治理 | EVIDENCE_GAP_AUTO_COLLECT=T · KNOWLEDGE_GOVERNANCE_FAIL_OPEN(C5) · MULTI_TURN_CACHE_ENABLE(C4) |
| 引用 | CITATION_VERIFIER_ENABLE · CITATION_NLI_ENABLE · **CITATION_NLI_ASYNC_ENABLE(C1)** · CITATION_STRUCTURED_OUTPUT · CITATION_REWRITE_ON_FAIL=T · CITATION_VERIFY_SIM_THRESHOLD=0.4 |
| 高级 | VLM_ENABLE · MEMORY_CAPACITY=500 · MCP_SERVER_PORT=9100 · TWIN_LAYOUT_PATH |
| 运维 | CONFIG_SOURCE=env/nacos · OTEL_SAMPLE_RATE · ALERT_WEBHOOK_TOKEN |

---

## 附 B · 端口与部署

| 项 | 值 |
|---|---|
| 后端 | 8001（改源码 `docker compose build backend && up -d`） · 前端 5173 |
| MySQL 3307 · Milvus 19530 · Neo4j 7474/7687 · MCP 9100 · 代理 7897 |
| 默认 admin/admin123 · Docker Compose 12 容器 |

---

## 附 C · 闭环总览

> **🛡️ 防幻觉四闸**：self_rag（拦非运维）→ crag v1/v2（分级纠错/拒答）→ citation_verifier 三层校验（格式/cosine/NLI）→ drop 警示 + rewrite 联动（C2 合并）。
> **🔄 自进化**：dislike → 聚类盲区 → AI 草稿 → 审核 → 回流降权。
> **🚨 主动运维**：realtime_event → Agent 建议 → 人工确认 → persistent_task → fault_prediction。
> **🛡️ 韧性**：双 RAG 热备 + 治理 fail-open + degraded 可观测 + backup。

**顶层设计**：网关（core/）管**安全**，RAG 引擎（rag/+routing/）管**质量**，高级能力（services/）管**业务**，横切（metrics/obs/nacos/otel）管**韧性**。
